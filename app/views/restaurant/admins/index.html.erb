<h1>Listing restaurant_admins</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @restaurant_admins.each do |restaurant_admin| %>
  <tr>
    <td><%= restaurant_admin.name %></td>
    <td><%= link_to 'Show', restaurant_admin %></td>
    <td><%= link_to 'Edit', edit_restaurant_admin_path(restaurant_admin) %></td>
    <td><%= link_to 'Destroy', restaurant_admin, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Admin', new_restaurant_admin_path %>
