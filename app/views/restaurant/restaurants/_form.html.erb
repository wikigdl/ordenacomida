<%= form_for(@restaurant_restaurant) do |f| %>
  <% if @restaurant_restaurant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@restaurant_restaurant.errors.count, "error") %> prohibited this restaurant_restaurant from being saved:</h2>

      <ul>
      <% @restaurant_restaurant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :address %><br />
    <%= f.text_area :address %>
  </div>
  <div class="field">
    <%= f.label :contact_info %><br />
    <%= f.text_area :contact_info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
